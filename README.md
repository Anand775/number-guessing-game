# Number Guessing Game 🎯

My first Python project! A CLI game where the user guesses a random number between 1-100.

## Features
- Random number generation using `random` module
- Input validation with `try-except` for error handling  
- `while` loop for continuous gameplay
- Hints: "Too high" / "Too low"

## How to Run
```bash
python loop.py
